---
title: نظرة عامة على دمج إصدار jrpm
---

> الفرع: `jrpm` ｜ الإصدار: jrpm-0.1.0 (موسوم، 2026-08-14)
> ناتج البناء: `openttd-jrpm` (اسم الملف التنفيذي)

## علاقة الإصدارات

```
                        jgrpp-0.73.1 (السلف المشترك)
                        /                 \
        jgrpp branch (63 commit)          pulsexlb px-patch (152 commit)
        ├ tracerestrict وغيرها من التحديثات   ├ jgrpp-decouple (فك القاطرة)
        ├ 5 وظائف ملكي (d4c45740)            └ jgrpp-multitile-airport (مطار معياري)
        └ تغيير اسم الإصدار jrpm-0.1.0 (425e7207)
                        \                 /
                        jrpm branch (merge 71fe214c + توافق cb9848b)
```

## محتوى الدمج

### 1. pulsexlb/OpenTTD-patches (px-patch كامل 152 commit) ← تم الدمج

| الوظيفة | الوصف | الملفات الرئيسية |
|---|---|---|
| **فك القاطرة (decouple)** | فصل/ربط القطارات: أوامر الفك، نقل العلامات، حدود طول/سرعة الربط، قاطرتين، ربط NewGRF، توجيه الربط (YAPF/NPF)، جدولة مستقلة بعد الفك | train_cmd.cpp، order_cmd.cpp، order_gui.cpp، train.h، yapf/npf |
| **مطار معياري (multitile-airport)** | إعادة هيكلة نظام المطار متعدد البلاطات: نظام أنواع air (air.h/air_type.h/newgrf_airtype.*)، جدولة PBS الجوية (pbs_air.*)، توجيه YAPF الجوي، `station.allow_modify_airports` (تعديل تخطيط المطار)، `gui.default_air_type`، سبرايتات مطار متعدد البلاطات | air.*، pbs_air.*، aircraft_cmd.cpp (إعادة هيكلة 3600 سطر)، airport_cmd/gui، station_cmd |

معالجة التعارض: تعارض ملفا رأس فقط (aircraft.h / airport.h)——إعادة هيكلة طيران pulsexlb حذفت أنواعاً ميتة **غير مشار إليها** في مساحة العمل (`VehicleAirFlags` bitset، `AirportMovingDataFlag`)، تم أخذ جانب الحذف من pulsexlb، وتم التأكد من عدم وجود ملفات أخرى تشير إليها.

### 2. Openttd-Cluster (مشروع Rust العنقودي للمستخدم) ← استعارة انتقائية

| التصحيح | المعالجة | الوصف |
|---|---|---|
| 0006 vanilla-native-server (توافق إصدارات متعددة للعملاء) | ✅ **تم الدمج** (cb9848b7) | خادم jrpm يقبل عملاء jrpm / jgrpp الأصلي (`jgrpp-`) / pulsexlb (`pxp`) في نفس الوقت؛ إصدار NewGRF لا يزال يُتحقق بدقة |
| 0001 revision-handshake / 0005 version-metadata | ✅ الفكرة تم اعتمادها | يستخدم jrpm سلسلة مراجعة موسومة مستقلة `jrpm-0.1.0`، مصافحة الشبكة معزولة عن jgrpp/pxp، مما يحقق "إصدار جديد مناسب للاتصال عبر الشبكة" |
| 0007 parallel-download (تجمع خيوط HTTP + تنزيل مجزأ Range، 30KB) | 📝 مرجعي بدون دمج | له نفس الموضوع مثل F1 لهذا المشروع "توازي على مستوى الملف + مرايا متعددة" ويغير نفس مجموعة الملفات؛ تم تسجيل **تجمع خيوط طبقة النقل/التنزيل المجزأ** من 0007 كاتجاه تحسين مستقبلي لـ F1 |
| 0002-0004 snapshot/command/FFI bridge | 📝 مرجعي معماري | يعتمد على كامل زمن تشغيل otc-engine Rust (ربط ثابت FFI)، هو "تكامل كلي بعيد المدى" وليس دمجاً على مستوى التصحيح؛ jrpm حالياً يبقى ثنائي C++ خالص |

### 3. حصري لـ jrpm (الوظائف الخمس السابقة، d4c45740) ← موجود بالفعل في فرع jrpm

تنزيل متوازي (مرايا متعددة + 4 جلسات متزامنة)، تجميع تلقائي، تلميح البناء، ذكاء اصطناعي يدرك اللعبة بأكملها (ScriptGlobal + GlobalAI)، إعدادات المرآة/خادم المحتوى.

## استراتيجية الشبكة ("إصدار جديد مناسب للاتصال عبر الشبكة")

- jrpm هو **إصدار موسوم**: `IsNetworkCompatibleVersion` يتطلب تطابقاً تاماً لسلسلة المراجعة ← **عميل jrpm يتصل فقط بخادم jrpm**، معزول تماماً عن jgrpp 0.73.x / pxp؛
- **الخادم يرخي**: خادم jrpm يقبل بالإضافة إلى ذلك عملاء `jgrpp-*` و `pxp*` (`IsJgrppNativeNetworkRevision` / `IsPxpNetworkRevision`، يجب أن يكون إصدار NewGRF متطابقاً)؛
- لذلك: مضيف خادم jrpm يفتح الخادم = يستقبل فقط لاعبي jrpm (افتراضي)؛ عندما يحتاج إلى توافق مع عملاء قدامى، يمكنه استقبال لاعبي jgrpp/pxp دون تغيير التكوين.

## البناء والتحقق (ينفذه المستخدم على جهازه)

```bash
# أول مرة (يحتاج CMake + تبعيات، راجع COMPILING.md)
cmake -B build ..
cmake --build build -j
# الناتج: build/openttd-jrpm.exe
```

أولويات التحقق:
1. `openttd-jrpm -v` يعرض `jrpm-0.1.0`؛
2. افتح حفظاً محلياً وشغّل 1-2 سنة (الدمج يتضمن تغييرات كبيرة في train/airport + إصدار الحفظ قد يزيد بسبب allow_modify_airports وغيرها)؛
3. بعد فتح الخادم: انضمام عميل jrpm ✓؛ تجربة انضمام عميل jgrpp 0.73.x الأصلي (متوقع الدخول، عندما يكون NewGRF متطابقاً)؛
4. فك القاطرة: أضف أوامر فك/ربط للقطار، تحقق من جدولة مستقلة للقطارين بعد الفك؛ مطار معياري: فعّل `station.allow_modify_airports` ثم عدّل تخطيط المطار؛
5. اختبار انحداري للوظائف الخمس السابقة (تنزيل متوازي، تجميع تلقائي، تلميح البناء، GlobalAI).

## المخاطر المعروفة

- **لم يتم التحقق من التجميع**: الكود المدمج+المعدّل لم يتم تجميعه على الجهاز (لا توجد سلسلة أدوات)، التجميع الأول على الجهاز الفعلي قد يكون لديه تغييرات واجهة مفقودة (خاصة التغييرات الكبيرة في أنظمة aircraft/airport/train الثلاثة)؛
- إصدار الحفظ: px-patch قد يزيد SLV (ذكر M9 gate إصدار savegame)، حفظ jrpm قد لا يُقرأ مع حفظ jgrpp 0.73.x (مثل اصطلاح jgrpp، متوافق مع حفظ trunk للإصدارات الأقدم)؛
- الدمج يجلب كل تاريخ pulsexlb، إذا كنت بحاجة إلى تتبع ملكية الوظائف استخدم `git log --oneline pulsexlb/px-patch`.