---
title: Общий обзор слияния версий jrpm
---

> Ветка: `jrpm` ｜ Версия: jrpm-0.1.0 (tagged, 2026-08-14)
> Результат сборки: `openttd-jrpm` (имя исполняемого файла)

## Связь версий

```
                        jgrpp-0.73.1 (общий предок)
                        /                 \
        jgrpp ветка (63 коммита)          pulsexlb px-patch (152 коммита)
        ├ tracerestrict и др. обновления    ├ jgrpp-decouple (смена локомотивов)
        ├ мои 5 функций (d4c45740)          └ jgrpp-multitile-airport (модульные аэропорты)
        └ переименование jrpm-0.1.0 (425e7207)
                        \                 /
                        jrpm ветка (merge 71fe214c + совместимость cb9848b)
```

## Содержание слияния

### 1. pulsexlb/OpenTTD-patches (px-patch полные 152 коммита) → объединены

| Функция | Описание | Основные файлы |
|---|---|---|
| **Смена локомотивов (decouple)** | Расцепление/сцепление поездов: приказы на расцепку, передача маршрутных листов, ограничения длины сцепки/скорости, двойная тяга, сцепка NewGRF, навигация сцепки (YAPF/NPF), независимое управление двумя поездами после расцепки | train_cmd.cpp, order_cmd.cpp, order_gui.cpp, train.h, yapf/npf |
| **Модульные аэропорты (multitile-airport)** | Переработка многосекционных аэропортов: система типов air (air.h/air_type.h/newgrf_airtype.*), PBS воздушная диспетчеризация (pbs_air.*), YAPF авиационная навигация, `station.allow_modify_airports` (изменение планировки аэропортов), `gui.default_air_type`, спрайты многосекционных аэропортов | air.*, pbs_air.*, aircraft_cmd.cpp (рефакторинг 3600 строк), airport_cmd/gui, station_cmd |

Обработка конфликтов: всего 2 конфликта заголовочных файлов (aircraft.h / airport.h) — pulsexlb авиационный рефакторинг удалил неиспользуемые мёртвые типы из рабочей области (`VehicleAirFlags` bitset, `AirportMovingDataFlag`), взята сторона pulsexlb, подтверждено отсутствие ссылок из других файлов.

### 2. Openttd-Cluster (проект Rust-кластера пользователя) → выборочные заимствования

| Патч | Обработка | Описание |
|---|---|---|
| 0006 vanilla-native-server (совместимость с несколькими версиями) | ✅ **Объединён** (cb9848b7) | Сервер jrpm одновременно принимает клиенты jrpm / оригинальный jgrpp (`jgrpp-`) / pulsexlb (`pxp`); версия NewGRF по-прежнему строго проверяется |
| 0001 revision-handshake / 0005 version-metadata | ✅ Идеи приняты | jrpm использует независимую tagged строку ревизии `jrpm-0.1.0`, рукопожатие изолировано от jgrpp/pxp, реализована «новая версия для удобной игры по сети» |
| 0007 parallel-download (HTTP пул потоков + Range-блочная загрузка, 30KB) | 📝 Справочно, не объединено | Та же тема, что и F1 «файловый параллелизм + несколько зеркал», затрагивает те же файлы; **транспортный пул потоков/блочная загрузка** 0007 записаны как направление будущего улучшения F1 |
| 0002-0004 snapshot/command/FFI мост | 📝 Архитектурная справка | Зависит от всей среды выполнения Rust otc-engine (FFI статическая линковка), относится к «дальней общей интеграции», а не к слиянию на уровне патчей; jrpm пока остаётся чистым C++ единым бинарником |

### 3. Собственные функции jrpm (предыдущие 5 функций, d4c45740) → уже в ветке jrpm

Параллельная загрузка (несколько зеркал + 4 параллельных сессии), автоматическая группировка, подсказка строительства, глобальный AI (ScriptGlobal + GlobalAI), настройки зеркал/сервера контента.

## Стратегия сетевой игры («новая версия для удобной игры по сети»)

- jrpm — **tagged-версия**: `IsNetworkCompatibleVersion` требует полного совпадения строки ревизии → **клиент jrpm подключается только к серверу jrpm**, полностью изолирован от jgrpp 0.73.x / pxp;
- **Серверное послабление**: сервер jrpm дополнительно принимает клиенты `jgrpp-*` и `pxp*` (`IsJgrppNativeNetworkRevision` / `IsPxpNetworkRevision`, версия NewGRF должна совпадать);
- Таким образом: хост jrpm открывает сервер = только для игроков jrpm (по умолчанию); при необходимости совместимости со старыми клиентами без изменения конфигурации принимает игроков jgrpp/pxp.

## Сборка и проверка (выполняется на машине пользователя)

```bash
# Первый раз (требуется CMake + зависимости, см. COMPILING.md)
cmake -B build ..
cmake --build build -j
# Результат: build/openttd-jrpm.exe
```

Приоритет проверки:
1. `openttd-jrpm -v` показывает `jrpm-0.1.0`;
2. Одиночная игра 1-2 года (слияние затрагивает крупные изменения train/airport + версия сохранения может быть увеличена из-за allow_modify_airports и т.д.);
3. После открытия сервера: подключение клиента jrpm ✓; попытка подключения оригинального клиента jgrpp 0.73.x (ожидается успех, если NewGRF совпадают);
4. Смена локомотивов: добавление приказов на расцепку/сцепку поезда, проверка независимого управления после расцепки; модульные аэропорты: включение `station.allow_modify_airports` и изменение планировки аэропорта;
5. Регрессия предыдущих 5 функций (параллельная загрузка, автоматическая группировка, подсказка строительства, GlobalAI).

## Известные риски

- **Не проверено на реальном железе**: код после слияния+модификации не компилировался на данной машине (нет цепочки инструментов), при первой компиляции могут быть пропущенные изменения интерфейсов (особенно серия крупных изменений aircraft/airport/train);
- Версия сохранения: px-patch мог увеличить SLV (M9 упоминает savegame version gate), сохранения jrpm и jgrpp 0.73.x могут не читать друг друга (следуя конвенции jgrpp, обратная совместимость с trunk сохранениями);
- Слияние привносит всю историю pulsexlb, для отслеживания принадлежности функций используйте `git log --oneline pulsexlb/px-patch`.